# Exclude local build outputs to speed Docker context
.next
node_modules

# Keep local env files out of image
.env
.env.*
!.env.example

# Git metadata and logs are not needed in container
.git
*.log
npm-debug.log*

# TypeScript cache files
*.tsbuildinfo
